import { MagicDirDirective } from './magic-dir.directive';

describe('MagicDirDirective', () => {
  it('should create an instance', () => {
    const directive = new MagicDirDirective();
    expect(directive).toBeTruthy();
  });
});
