import { MagicBoxDirective } from './magic-box.directive';

describe('MagicBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new MagicBoxDirective();
    expect(directive).toBeTruthy();
  });
});
